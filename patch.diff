(cd "$(git rev-parse --show-toplevel)" && git apply --3way <<'EOF'
diff --git a/alm/core/include/alm/core/time_stencil.hpp b/alm/core/include/alm/core/time_stencil.hpp
index 1e2143af0f5952c42797fd3c3c16582e60a9f78f..a3c8f1c5e7f2d2f8c9a7d7c7e9d5a9e8f1234567 100644
--- a/alm/core/include/alm/core/time_stencil.hpp
+++ b/alm/core/include/alm/core/time_stencil.hpp
@@ -12,7 +12,7 @@ struct TimeStencil {
     struct PressureSnapshot {
         std::size_t rotation_index;
-        std::size_t write_epoch;
+        std::size_t rotation_epoch;
         std::size_t writes_captured;
         bool had_overwrite;
     };
@@ -45,7 +45,7 @@ private:
     std::size_t i_future_;
 
     std::atomic<std::size_t> rotation_count_;
-    std::atomic<std::size_t> future_write_epoch_;
+    std::atomic<std::size_t> rotation_epoch_;
     std::atomic<std::size_t> future_write_count_;
     std::atomic<std::size_t> future_write_offset_;
     std::atomic<bool> future_had_overwrite_;
diff --git a/alm/core/src/time_stencil.cpp b/alm/core/src/time_stencil.cpp
index d66578cfa2cca332e05a4179f93076fcd8e693ea..c9b0e1234abcdeffedcba9876543210fedcba999 100644
--- a/alm/core/src/time_stencil.cpp
+++ b/alm/core/src/time_stencil.cpp
@@ -1,8 +1,6 @@
 #include "alm/core/time_stencil.hpp"
 
-#include <algorithm>
-
 TimeStencil::TimeStencil(TensorCluster& cluster)
     : cluster_(&cluster),
       i_stable_(0),
@@ -11,7 +9,7 @@ TimeStencil::TimeStencil(TensorCluster& cluster)
       i_now_(2),
       i_future_(3),
       rotation_count_(0),
-      future_write_epoch_(0),
+      rotation_epoch_(0),
       future_write_count_(0),
       future_write_offset_(0),
       future_had_overwrite_(false) {}
@@ -63,25 +61,25 @@ void TimeStencil::ingest_write_future(Value value, std::size_t count) {
 }
 
 TimeStencil::PressureSnapshot TimeStencil::tick_compute() {
-    const std::size_t captured_rotation = rotation_count_.load(std::memory_order_relaxed);
-    const std::size_t captured_epoch = future_write_epoch_.load(std::memory_order_relaxed);
+    const std::size_t tick_id =
+        rotation_count_.fetch_add(1, std::memory_order_relaxed);
+    const std::size_t captured_epoch =
+        rotation_epoch_.load(std::memory_order_relaxed);
     const std::size_t captured_writes =
         future_write_count_.exchange(0, std::memory_order_relaxed);
     const bool captured_overwrite =
         future_had_overwrite_.exchange(false, std::memory_order_relaxed);
 
     rotate_once();
 
-    rotation_count_.fetch_add(1, std::memory_order_relaxed);
-    future_write_epoch_.fetch_add(1, std::memory_order_relaxed);
+    rotation_epoch_.fetch_add(1, std::memory_order_relaxed);
     future_write_offset_.store(0, std::memory_order_relaxed);
 
-    return PressureSnapshot{.rotation_index = captured_rotation,
-                             .write_epoch = captured_epoch,
+    return PressureSnapshot{.rotation_index = tick_id,
+                             .rotation_epoch = captured_epoch,
                              .writes_captured = captured_writes,
                              .had_overwrite = captured_overwrite};
 }
diff --git a/alm/core/tests/staged_future_pressure_test.cpp b/alm/core/tests/staged_future_pressure_test.cpp
index 516bba33453be5a49b81f05b457a93e3a8fa8cef..aa11112222333344445555666677778888999900 100644
--- a/alm/core/tests/staged_future_pressure_test.cpp
+++ b/alm/core/tests/staged_future_pressure_test.cpp
@@ -14,7 +14,7 @@ int main() {
     auto snapshot = stencil.tick_compute();
     assert(snapshot.rotation_index == 0);
-    assert(snapshot.write_epoch == 0);
+    assert(snapshot.rotation_epoch == 0);
     assert(snapshot.writes_captured == 10);
     assert(snapshot.had_overwrite == false);
 
@@ -24,7 +24,7 @@ int main() {
     snapshot = stencil.tick_compute();
     assert(snapshot.rotation_index == 1);
-    assert(snapshot.write_epoch == 1);
+    assert(snapshot.rotation_epoch == 1);
     assert(snapshot.writes_captured == heavy_count);
     assert(snapshot.had_overwrite == true);
 
@@ -34,7 +34,7 @@ int main() {
     snapshot = stencil.tick_compute();
     assert(snapshot.rotation_index == 2);
-    assert(snapshot.write_epoch == 2);
+    assert(snapshot.rotation_epoch == 2);
     assert(snapshot.writes_captured == 4);
     assert(snapshot.had_overwrite == false);
EOF
)